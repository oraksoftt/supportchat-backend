namespace SupportChat.Backend.Helpers;

public static class LocalizationHelper
{
    // Placeholder – will be used later with the localization table
    public static string GetTranslation(string key, string languageCode)
    {
        // In real implementation, fetch from cache/DB
        return key; 
    }
}